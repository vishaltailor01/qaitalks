{
  "scripts": {
    "build": "npm --prefix next-app run build:cloudflare",
    "postinstall": "npm --prefix next-app run postinstall",
    "type-check": "npm --prefix next-app run type-check"
  },
  "devDependencies": {
    "@prisma/client": "^6.19.2",
    "@types/node": "^25.2.1",
    "prisma": "^6.19.2",
    "typescript": "^5.9.3"
  }
}
